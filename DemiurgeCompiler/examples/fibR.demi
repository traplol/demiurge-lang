//recursive Fibonacci sequence
extern func printi(int):void;

func fib(x : int) : int {
    if (x < 3)
        return 1;
    else
        return fib(x-1) + fib(x-2);
}


func main() : int {
    printi(fib(40));
    return 0;
}
